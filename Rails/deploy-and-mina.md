### deploy

```shell
mina deploy
```

### ssh to server

```shell
mina ssh
cd current
```

### rails console or rake task

( At current/ directory)

```shell
bundle exec rake db:seed
bundle exec rails c
```